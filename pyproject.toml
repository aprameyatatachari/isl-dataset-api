[project]
name = "isl-dataset-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "cloakbrowser>=0.5.7",
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.34.0",
    "yt-dlp>=2024.12.13",
]

# Keep local formatting identical to the CI lint job (.github/workflows/ci-cd.yml).
[tool.black]
line-length = 88

[tool.isort]
profile = "black"
line_length = 88
